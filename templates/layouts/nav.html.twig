{% if is_granted('ROLE_ADMIN')==true %}
    <nav class="navbar navbar-expand-sm navbar-dark bg-danger mb-4">
{% else %}
    <nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-4">
{% endif %}
    <div class="container">
        <h3><a class="navbar-brand" style="font-size: larger" href="{{ path('app_home') }}">Oui.sorties !</a></h3>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample03" aria-controls="navbarsExample03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarsExample03">
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_sortie_create') }}">Créer une sortie</a>
                    </li>
                    <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_lieu') }}">Lieux</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_account') }}">Mon profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </li>
                {% if is_granted('ROLE_ADMIN')==true %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_admin_campus') }}">Campus</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path ('app_admin_ville')}}">Villes</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_admin_participant') }}">Participants</a>
                    </li>
                {% endif %}

                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{  path('app_login') }}">Se connecter</a>
                    </li>
                {%  endif %}
            </ul>
        </div>
    </div>
</nav>