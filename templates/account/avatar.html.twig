{% extends 'base.html.twig' %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h1 class="my-3">Mon avatar</h1>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="d-grid gap-2">
                <button type="submit" class="btn btn-primary">Envoyer</button>
            </div>
            {{ form_end(form) }}
            <p>
                <a href={{ path('app_account') }}#">Retour</a>
            </p>
        </div>
    </div>

{% endblock %}

{% block title %}

{% endblock %}

